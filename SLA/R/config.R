# Service Level Agreement configurations ---------------------------------------
cfg = list(
  folders = list(
    input_folder = "/home/luga/Dropbox/Git/SLA/dataset/",
    preprocessed ="/home/luga/Dropbox/Git/SLA/preprocessed/",
    processed = "/home/luga/Dropbox/Git/SLA/processed/",
    postprocessed = "/home/luga/Dropbox/Git/SLA/postprocessed/",
    database = "/home/luga/Dropbox/Git/SLA/db/"
  ),
  pre_process = list(
    filename = "ticket_cientista.csv",
    exist_header = T,
    separator = ";",
    range_within_year = T,
    initial_date = "2019-01-01 00:00:00",
    end_date = "2019-02-26 23:59:59",
    format_date = T,
    closed_ticket_col = "callStatus",
    closed_ontime_col = "onTimeSolution",
    closeDate_col = "callCloseDate",
    closeDateTime_col = "closeDateTime",
    convert_int_to_date = T,
    slaStatus_col = "slaStatus",
    customers_col = "customerCode",
    callNumber_col = "callNumber",
    threads = 7
  ),
  process = list(
    customers = c("215"),
    days_prediction = c("2019-02-27", "2019-02-28", "2019-03-01", "2019-03-02"),
    linear_regression = T,
    use_linear_selfmade = T,
    use_linear_blackbox = T,
    frequentist_regression = T,
    bayesian_regression = T,
    bayes_sd = 1,
    bayes_nsteps = 100000,
    mcmc_burnin = 5000,
    poly_regression = T,
    use_poly_selfmade = T,
    use_poly_blackbox = T,
    poly_degree = c(1, 3, 4),
    decisiontree_regression = F,
    accuracy_test = F
  ),
  post_process = list(
    only_postprocess = F,
    plot_calls = list(
      run_module = T,
      axes_names = c("Number of calls", "Calendar"),
      label_size = 2.0,
      main_size = 2.0,
      axis_size = 2.0,
      test_size = 2.0,
      symbol_size = 1.2,
      color = "red",
      plot_type = "h",
      plot_size = c(800, 600),
      plot_format = "png",
      plot_symbol = 19,
      frame_plot = T,
      margin = 1.1,
      line_width = 3,
      ylim = c(0, 1)
    ),
    plot_closed_calls = list(
      run_module = T,
      axes_names = c("Number of closed calls", "Calendar"),
      label_size = 2.0,
      main_size = 2.0,
      axis_size = 2.0,
      test_size = 2.0,
      symbol_size = 1.2,
      color = "red",
      plot_type = "h",
      plot_size = c(800, 600),
      plot_format = "png",
      plot_symbol = 19,
      frame_plot = T,
      margin = 1.1,
      line_width = 3,
      ylim = c(0, 1)
    ),
    plot_sla = list(
      run_module = T,
      axes_names = c("Service Level Agreement", "Calendar"),
      label_size = 2.0,
      main_size = 2.0,
      axis_size = 2.0,
      test_size = 2.0,
      symbol_size = 1.2,
      color = "red",
      plot_type = "h",
      plot_size = c(800, 600),
      plot_format = "png",
      plot_symbol = 19,
      frame_plot = T,
      margin = 1.1,
      line_width = 3,
      ylim = c(0, 1)
    ),
    plot_models = T
  )
)
